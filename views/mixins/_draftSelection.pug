mixin draftSelection(players)
    .col-5
        select#search.form-control(name='from[]' size='8' multiple='multiple')
            each player in players
                option(value= player.personId) #{player.temporaryDisplayName}
    .d-grid.gap-2.col-2.mx-aut
        button#search_rightAll.btn.btn-primary(type='button')
            i.bi.bi-forward-fill
        button#search_rightSelected.btn.btn-primary(type='button')
            i.glyphicon.glyphicon-chevron-right
        button#search_leftSelected.btn.btn-primary(type='button')
            i.glyphicon.glyphicon-chevron-left
        button#search_leftAll.btn.btn-primary(type='button')
            i.glyphicon.glyphicon-backward
    .col-5
        select#search_to.form-control(name='to[]' size='8' multiple='multiple')
    script(type='text/javascript').
        $(document).ready(function($) {
            $('#search').multiselect({
                search: {
                    left: '<input type="text" name="q" class="form-control" placeholder="Search..." />',
                    right: '<input type="text" name="q" class="form-control" placeholder="Search..." />',
                },
                fireSearch: function(value) {
                    return value.length > 3;
                }
            });
        });
